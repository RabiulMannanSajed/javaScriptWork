/* const products = [
    { name: 'watch', price: 3000, brand: 'omex', color: 'silver' },
    { name: 'Sunglass', price: 500, brand: 're bon', color: 'black' },
    { name: 'laptop', price: 8000, brand: 'lenovo', color: 'gray' },
    { name: 'pen', price: 55, brand: 'metador', color: 'pink' },
]
const cheap = products.filter(product => product.price <= 5000);
console.log(cheap);

const numbers = [23, 54, 58, 6, 45];
numbers.forEach(number => console.log(number));
numbers.map(number => number * 2);
*/
const obj = { a: 1 };
console.log(Object.keys(obj).length === 0);

